{
  // Cloudflare Worker with static assets — same pattern as the newer CivicLearn sites.
  // Dashboard: Workers & Pages → Create → Import a repository → this repo. Build command: (none). Deploy: npx wrangler deploy
  "name": "kotiopetus",
  "compatibility_date": "2026-09-01",
  "assets": {
    "directory": "./public",
    "not_found_handling": "single-page-application"
  }
}
